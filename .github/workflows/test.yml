name: test

on:
  push:
  pull_request:

jobs:
  cargo:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.82.0
      - name: Test (locked)
        run: cargo test --locked
